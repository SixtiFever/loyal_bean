import React from 'react';
import {Text, View, Button} from 'react-native';

const ShopRegister = ({navigation}) => {
    return (
        <View>
            <Button title='Register a shop' />
            <Button title='Sign into your shop' />
        </View>
    )
}

export default ShopRegister;